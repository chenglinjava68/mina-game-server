<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="Mapper_User">

	<insert id="add" parameterType="User" useGeneratedKeys="true">
		INSERT INTO gs_user(
			username,
			password,
			createDate) 
		VALUES(
			#{username},
			#{password},
			#{createDate})
		<!-- 
		<selectKey keyProperty="id" resultType="int" order="AFTER">
			select LAST_INSERT_ID() 
		</selectKey>
		 -->
	</insert>
	
	<select id="selectAll" resultType="User">
		SELECT * FROM gs_user
	</select>
	
	<!-- 
	<select id="selectAllByPage" parameterType="Pagination" resultType="User">
		SELECT * FROM stat_gem order by createDate desc 
		limit #{start},#{limit}
	</select>
	 -->
</mapper>