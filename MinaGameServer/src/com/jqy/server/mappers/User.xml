<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="Mapper_User">

	<insert id="add" parameterType="User" useGeneratedKeys="true">
		INSERT INTO gs_user(
			username,
			password,
			createDate) 
		VALUES(
			#{username},
			#{password},
			#{createDate})
	</insert>
	
	<select id="selectAll" resultType="User">
		SELECT * FROM gs_user
	</select>
	
	<select id="login" parameterType="map" resultType="User">
		SELECT * FROM gs_user WHERE username=#{username} AND password=#{password}
	</select>
	
	<select id="selectAllByPage" parameterType="map" resultType="User">
		SELECT * FROM gs_user ORDER BY createDate DESC 
		LIMIT #{start},#{limit}
	</select>
</mapper>